apiVersion: v1
kind: Service
metadata:
  name: inventario-blue-green
  labels:
    app: inventario-bg
spec:
  type: NodePort
  selector:
    app: inventario-bg
    slot: blue
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
