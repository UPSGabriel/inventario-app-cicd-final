apiVersion: v1
kind: Service
metadata:
  name: inventario-app
  labels:
    app: inventario-app
spec:
  type: NodePort

  selector:
    app: inventario-app

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
